let verduras = ["Apio", "Tomate", "Repollo"];

function vidaSaludable(verduras) {
    for (let i = 0; i < verduras.length; i++) {

        console.log(`Empieza una vida saludable con ${verduras}`);
        
    }
    
};